<h1> <%= @song.name %> </h1>
<h2>by <a href="/artists/<%= @song.artist.slug %>">
      <%= @song.artist.name %></a>
</h2>

  <% @song.genres.each do |g| %>
    <a href="/genres/<%= g.slug %>"><%= g.name %></a>
  <% end %>
