<h1> <%= @genre.name %> </h1>

<h2>Songs:
  <% @genre.songs.each do |s| %>
    <a href="/songs/<%= s.slug %>"><%= s.name %></a> - <a href="/artists/<%= s.artist.slug %>"><%= s.artist.name %></a>
    
<br>
  <% end %>
</h2>
