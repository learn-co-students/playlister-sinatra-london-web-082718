<h1> <%= @artist.name %> </h1>

<h2>Songs:
  <% @artist.songs.each do |s| %>
    <a href="/songs/<%= s.slug %>"><%= s.name %></a> -
    <% s.genres.each do |g| %>
      <a href="/genres/<%= g.slug %>"><%= g.name %></a>
    <% end %><br>
  <% end %>
</h2>
